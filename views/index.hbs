<ul class="grid">
  {{#each jokes}}
  <li class=grid__item>
    <article class="card">
      <p class="card__body">{{body}}</p>
      <footer class="card__footer"><cite>{{username}}</cite></footer>
    </article>
  </li>
  {{/each}}
</ul>
